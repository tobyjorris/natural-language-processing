import { postDataToServer } from "../client";
import { describe } from '@jest/globals';

describe("Testing the post functionality", () => {
    test("Testing the postDataToServer() function", async () => {
    })
})
